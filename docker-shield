FROM python:3.11-slim

WORKDIR /app
COPY app/ /app/

# Force critical package vulnerabilities directly into container layers
RUN pip install urllib3==1.24.1 Django==2.0.0

EXPOSE 5000
CMD ["python", "server.py"]