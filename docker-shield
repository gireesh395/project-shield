FROM python:3.11-slim

WORKDIR /app
COPY app/ /app/

# Install an outdated OS package with known CRITICAL CVEs
RUN apt-get update && apt-get install -y --no-install-recommends wget=1.21-1+deb11u1 || true

EXPOSE 5000
CMD ["python", "server.py"]